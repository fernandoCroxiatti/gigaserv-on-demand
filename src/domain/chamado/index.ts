/**
 * Chamado Domain Module
 * Pure business logic - no React, no external dependencies
 * Can be reused in native mobile apps (React Native, Swift, Kotlin)
 */

// Types
export * from './types';

// Constants
export * from './constants';

// Validation rules
export * from './validation';

// Mappers (DB ↔ Domain)
export * from './mappers';
